#include<iostream>
#include<set>
#include<algorithm>
using namespace std;

int main()
{
	set<int> st;
	for (int i = 0; i < 5; i++)
		st.insert(i);

	if (st.find(3) == st.end())
		cout << "没找到\n";
	else
		cout << *st.find(3) << endl;
	cout << st.count(3) << endl;
	return 0;
}
