#include<iostream>
using namespace std;
#include<stack>

int main()
{
	stack<int> st;
	st.push(9);
	st.push(8);
	st.push(7);

	cout << st.size() << endl;

	while (!st.empty())
	{
			cout << st.top() << " ";
			st.pop();
	}

	return 0;
}
